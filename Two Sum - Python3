class Solution:
    def twoSum(self, nums: List[int], target: int) -> List[int]:
        numList = {}
        for i, num in enumerate(nums):
            comp = target - num
            if comp in numList:
                return [numList[comp], i]
            numList[num] = i
